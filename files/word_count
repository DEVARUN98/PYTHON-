To quickly evaluate the value of any expression while debugging the program,
hold Alt (Windows and Linux) or Alt(Option) (macOS) and click this expression
to see its value and calculate it, call a method, and so on.